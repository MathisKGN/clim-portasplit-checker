# --- Moniteur de stock multi-enseignes (stockmonitor/) ----------------------
# Leroy Merlin (défaut) : scan en HTTP pur (curl_cffi), avec un warmup Camoufox
#   pour établir la session. Les cookies du warmup sont réutilisés par curl_cffi.
curl_cffi>=0.7
beautifulsoup4>=4.12
lxml>=5.0
# Camoufox : navigateur utilisé pour le warmup (mode HTTP par défaut) et pour
# le fallback --use-camoufox.
# Camoufox 0.4.11 est incompatible avec Playwright >= 1.51
# (erreur "viewport.isMobile not described in this scheme"). On épingle 1.50.0.
camoufox[geoip]>=0.4.11
playwright==1.50.0

# Castorama / Darty / Amazon : API JSON, aucun navigateur requis (a priori)
requests>=2.31
